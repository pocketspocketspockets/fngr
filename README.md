# fngr

An implementation of finger built for a more modern world than the original one

this is very sloppily thrown together

## how it expects urls

`/login?n=username&k=userkey`

`/finger?u=username`

---
supports `login`, `keepalive`/`bump`, `finger`, `list`/`fingerall`
